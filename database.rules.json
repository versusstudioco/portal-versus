{
  "rules": {
    ".read": false,
    ".write": false,
    "vsportal": {
      ".read": "auth != null",
      ".write": false,
      "db": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren()"
      },
      "creds": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren()"
      },
      "gestor": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
