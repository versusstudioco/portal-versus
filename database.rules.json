{
  "rules": {
    ".read": false,
    ".write": false,
    "vsportal": {
      ".read": true,
      ".write": false,
      "db": {
        ".write": true,
        ".validate": "newData.hasChildren()"
      },
      "creds": {
        ".write": true,
        ".validate": "newData.hasChildren()"
      }
    }
  }
}
